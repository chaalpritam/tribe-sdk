import { describe, it, expect } from "vitest";

describe("TribeClient", () => {
  it("should create a devnet client", () => {
    // TODO: Create mock AnchorProvider and verify TribeClient.forDevnet()
    expect(true).toBe(true);
  });

  it("should create a mainnet client", () => {
    // TODO: Create mock AnchorProvider and verify TribeClient.forMainnet()
    expect(true).toBe(true);
  });

  it("should accept a custom ExecutionProvider", () => {
    // TODO: Verify that a custom ExecutionProvider is used
    expect(true).toBe(true);
  });
});
